sal({
    selector: "sall-ele",
    // threshold: 1,
    once: true,
    animation: "slide-down"
});

// let listOfOutputsElement =  document.getElementById("listOfOutputs");
// listOfOutputsElement.scrollTop = listOfOutputsElement.scrollHeight;
// // console.log(listOfOutputsElement.scrollHeight);
// console.log(listOfOutputsElement.scrollTop);

// document.getElementById("lastItem").scrollIntoView({ behavior: 'smooth', block: 'end' });

// alert("don")

    document.getElementById("lastItem").scrollIntoView();

// var list = document.querySelectorAll("ul li");
// var lastElement = list[list.length - 1]
// setTimeout(() => {
// }, 0)